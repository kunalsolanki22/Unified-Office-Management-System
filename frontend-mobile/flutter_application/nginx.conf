server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        
        # This is for Flutter web to handle client-side routing properly
        try_files $uri $uri/ /index.html;
    }
}
