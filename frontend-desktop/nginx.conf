server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        
        # This is for React Router to handle client-side routing
        try_files $uri $uri/ /index.html;
    }
}
